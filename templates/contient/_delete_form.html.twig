<form method="post" action="{{ path('app_contient_delete', {'id': contient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contient.id) }}">
    <button class="btn">Delete</button>
</form>
